extends layout

block content
  div
    ol(class="breadcrumb")
      li
        a(href="/") Home
  div(class="jumbotron")
    h1 将棋の棋譜保存
    ul
      li アルバムを作ってその中に将棋の棋譜を保存できます。
      li 棋譜ジェネレータで、.kif形式の棋譜を生成できます。
    if !user
      a(class="btn btn-primary" href="/login") Login Page
  if user
    div
      a(class="btn btn-default" href="/collections/newC") New Album
    - var hasCollection = collections.length > 0
    if hasCollection
      h3 Album List
      table(class="table")
        tr
          th Created-by
          th Album Title
        each collection in collections
          tr
            td #{collection.user.username}
            td
              a(class="btn btn-link"
                href="/collections/#{collection.collectionId}") #{collection.collectionName}
  hr
  a(class="btn btn-link btn-lg" id="toTop")
    span(class="glyphicon glyphicon-chevron-up")